node_modules/
.env
*.csv
.DS_Store
.tokens.json

# Cloudflare tunnel credentials — never commit
*.json
!package.json
!package-lock.json

# Openclaw — sensitive config, never commit
.email-config
*.email-config
openclaw-service.yaml
openclaw*.yaml
.openclaw-env
